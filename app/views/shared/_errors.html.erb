<% if @book.errors.any? %>
  <div class="row">
    <div class="col-xs-10 col-xs-offset-1">
      <div class="panel panel-danger">
        <div class="panel-heading">
          <h2 class="panel-title"><%= pluralize(@book.errors.count, "error") %> prohibited this book from being saved:</h2>
          <div class="panel-body">
            <ul>
              <% @book.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
              <% end %>
            </ul>
          </div>
        </div>
      </div>
    </div>
  </div>
<% end %>